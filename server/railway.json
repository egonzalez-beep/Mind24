{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && npm run seed && node src/server.js"
    "restartPolicyType": "ON_FAILURE"
  }
}
